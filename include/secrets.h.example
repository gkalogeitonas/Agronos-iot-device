#pragma once

// ==================== DEVICE CREDENTIALS ====================
// These are sensitive values that uniquely identify this device.
// Copy this file to secrets.h and update with your actual device credentials.
// The secrets.h file is gitignored and should never be committed to version control.
// ============================================================

constexpr const char* DEFAULT_UUID   = "Test-Device-1";
constexpr const char* DEFAULT_SECRET = "Test-Device-1";
